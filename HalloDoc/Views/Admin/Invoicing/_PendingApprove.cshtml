@{
    Layout = null;
}
@model List<HalloDoc.Entity.DataModels.Timesheet>
@if (Model.Count != 0)
{
    <h3 class="mt-3">Pending Bi-Weekly Timesheet</h3>
    <table class="table my-4">
        <thead>
            <tr class="table-secondary">
                <th>Start Date</th>
                <th>End Date</th>
                <th>Status</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Startdate</td>
                    <td>@item.Enddate</td>
                    <td>Pending</td>
                    <td>
                        <a href="~/Invoicing/Timesheet?PhysicianId=@item.Physicianid&StartDate=@item.Startdate.ToString().Substring(6)/@item.Startdate.ToString().Substring(3,2)/@item.Startdate.ToString().Substring(0,2)" class="btn mx-1 btn-sm btn-outline-info view-btn">
                            Approve
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}